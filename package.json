{
  "name": "mytapfeed-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && CI=false npm run build",
    "vercel-build": "npm run build"
  }
}
